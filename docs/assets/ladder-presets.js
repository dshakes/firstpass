// GENERATED by `cargo test -p firstpass-proxy presets` — do not edit by hand.
// Source of truth: crates/firstpass-proxy/src/onboard.rs (render_config).
// Regenerate: FIRSTPASS_WRITE_PRESETS=1 cargo test -p firstpass-proxy presets
window.FP_LADDER_PRESETS = {
  "anthropic": {
    "json": {
      "observe": "# firstpass.toml — written by `firstpass onboard`. Re-run onboard to regenerate.\n#   FIRSTPASS_MODE=observe FIRSTPASS_CONFIG=./firstpass.toml firstpass up\n\n[[route]]                   # routes match top to bottom; first match wins\nmatch  = {}                 # everything\nmode   = \"observe\"\nladder = [\"anthropic/claude-haiku-4-5\", \"anthropic/claude-sonnet-5\"]\ngates  = [\"json-valid\", \"extract-shape\"]\n\n[[gate]]\nid         = \"extract-shape\"\nschema     = { type = \"object\", required = [\"id\", \"total\"] }\non_abstain = \"fail_closed\"\n\n# observe: every request is forwarded unchanged and a receipt is written off\n# the hot path. Nothing routes differently until mode = \"enforce\".\n",
      "enforce": "# firstpass.toml — written by `firstpass onboard`. Re-run onboard to regenerate.\n#   FIRSTPASS_MODE=enforce FIRSTPASS_CONFIG=./firstpass.toml firstpass up\n\n[[route]]                   # routes match top to bottom; first match wins\nmatch  = {}                 # everything\nmode   = \"enforce\"\nladder = [\"anthropic/claude-haiku-4-5\", \"anthropic/claude-sonnet-5\"]\ngates  = [\"json-valid\", \"extract-shape\"]\n\n[[gate]]\nid         = \"extract-shape\"\nschema     = { type = \"object\", required = [\"id\", \"total\"] }\non_abstain = \"fail_closed\"\n\n[escalation]\nmax_rungs_per_request = 2   # one rung up, never a runaway\n",
    },
    "code": {
      "observe": "# firstpass.toml — written by `firstpass onboard`. Re-run onboard to regenerate.\n#   FIRSTPASS_MODE=observe FIRSTPASS_CONFIG=./firstpass.toml firstpass up\n\n[[route]]                   # routes match top to bottom; first match wins\nmatch  = {}                 # everything\nmode   = \"observe\"\nladder = [\"anthropic/claude-haiku-4-5\", \"anthropic/claude-sonnet-5\"]\ngates  = [\"json-valid\", \"unit-tests\"]\n\n[[gate]]\n# REPLACE ME. A gate reads the candidate as JSON on stdin and prints\n#   {\"verdict\":\"pass|fail|abstain\", \"score\"?: 0.0-1.0, \"reason\"?: \"...\"}\n# on stdout. Wrap your real test command in that contract — a bare `cargo test`\n# or `npm test` will not work, it would abstain on every request.\n# `firstpass doctor` fails on this line until you point it at your wrapper.\nid  = \"unit-tests\"\ncmd = [\"your-test-runner\", \"--from-stdin\"]\n\n# observe: every request is forwarded unchanged and a receipt is written off\n# the hot path. Nothing routes differently until mode = \"enforce\".\n",
      "enforce": "# firstpass.toml — written by `firstpass onboard`. Re-run onboard to regenerate.\n#   FIRSTPASS_MODE=enforce FIRSTPASS_CONFIG=./firstpass.toml firstpass up\n\n[[route]]                   # routes match top to bottom; first match wins\nmatch  = {}                 # everything\nmode   = \"enforce\"\nladder = [\"anthropic/claude-haiku-4-5\", \"anthropic/claude-sonnet-5\"]\ngates  = [\"json-valid\", \"unit-tests\"]\n\n[[gate]]\n# REPLACE ME. A gate reads the candidate as JSON on stdin and prints\n#   {\"verdict\":\"pass|fail|abstain\", \"score\"?: 0.0-1.0, \"reason\"?: \"...\"}\n# on stdout. Wrap your real test command in that contract — a bare `cargo test`\n# or `npm test` will not work, it would abstain on every request.\n# `firstpass doctor` fails on this line until you point it at your wrapper.\nid  = \"unit-tests\"\ncmd = [\"your-test-runner\", \"--from-stdin\"]\n\n[escalation]\nmax_rungs_per_request = 2   # one rung up, never a runaway\n",
    },
    "prose": {
      "observe": "# firstpass.toml — written by `firstpass onboard`. Re-run onboard to regenerate.\n#   FIRSTPASS_MODE=observe FIRSTPASS_CONFIG=./firstpass.toml firstpass up\n\n[[route]]                   # routes match top to bottom; first match wins\nmatch  = {}                 # everything\nmode   = \"observe\"\nladder = [\"anthropic/claude-haiku-4-5\", \"anthropic/claude-sonnet-5\"]\ngates  = [\"non-empty\", \"judge\"]\n\n[[gate]]                    # judge sits OUTSIDE the ladder: maker != checker\nid    = \"judge\"\njudge = { model = \"anthropic/claude-opus-4-8\", threshold = 0.7, rubric = \"The response fully and correctly resolves the request, with no errors.\" }\n\n# observe: every request is forwarded unchanged and a receipt is written off\n# the hot path. Nothing routes differently until mode = \"enforce\".\n",
      "enforce": "# firstpass.toml — written by `firstpass onboard`. Re-run onboard to regenerate.\n#   FIRSTPASS_MODE=enforce FIRSTPASS_CONFIG=./firstpass.toml firstpass up\n\n[[route]]                   # routes match top to bottom; first match wins\nmatch  = {}                 # everything\nmode   = \"enforce\"\nladder = [\"anthropic/claude-haiku-4-5\", \"anthropic/claude-sonnet-5\"]\ngates  = [\"non-empty\", \"judge\"]\n\n[[gate]]                    # judge sits OUTSIDE the ladder: maker != checker\nid    = \"judge\"\njudge = { model = \"anthropic/claude-opus-4-8\", threshold = 0.7, rubric = \"The response fully and correctly resolves the request, with no errors.\" }\n\n[escalation]\nmax_rungs_per_request = 2   # one rung up, never a runaway\n",
    },
    "mixed": {
      "observe": "# firstpass.toml — written by `firstpass onboard`. Re-run onboard to regenerate.\n#   FIRSTPASS_MODE=observe FIRSTPASS_CONFIG=./firstpass.toml firstpass up\n\n[[route]]                   # routes match top to bottom; first match wins\nmatch  = {}                 # everything\nmode   = \"observe\"\nladder = [\"anthropic/claude-haiku-4-5\", \"anthropic/claude-sonnet-5\"]\ngates  = [\"non-empty\", \"uncertainty\"]\n\n[[gate]]                    # k samples; agreement becomes the score\nid          = \"uncertainty\"\nconsistency = { model = \"anthropic/claude-haiku-4-5\", k = 3, threshold = 0.6 }\n\n# observe: every request is forwarded unchanged and a receipt is written off\n# the hot path. Nothing routes differently until mode = \"enforce\".\n",
      "enforce": "# firstpass.toml — written by `firstpass onboard`. Re-run onboard to regenerate.\n#   FIRSTPASS_MODE=enforce FIRSTPASS_CONFIG=./firstpass.toml firstpass up\n\n[[route]]                   # routes match top to bottom; first match wins\nmatch  = {}                 # everything\nmode   = \"enforce\"\nladder = [\"anthropic/claude-haiku-4-5\", \"anthropic/claude-sonnet-5\"]\ngates  = [\"non-empty\", \"uncertainty\"]\n\n[[gate]]                    # k samples; agreement becomes the score\nid          = \"uncertainty\"\nconsistency = { model = \"anthropic/claude-haiku-4-5\", k = 3, threshold = 0.6 }\n\n[escalation]\nmax_rungs_per_request = 2   # one rung up, never a runaway\n",
    },
  },
  "openai": {
    "json": {
      "observe": "# firstpass.toml — written by `firstpass onboard`. Re-run onboard to regenerate.\n#   FIRSTPASS_MODE=observe FIRSTPASS_CONFIG=./firstpass.toml firstpass up\n\n[[route]]                   # routes match top to bottom; first match wins\nmatch  = {}                 # everything\nmode   = \"observe\"\nladder = [\"openai/gpt-4.1-mini\", \"openai/gpt-5.5\"]\ngates  = [\"json-valid\", \"extract-shape\"]\n\n[[gate]]\nid         = \"extract-shape\"\nschema     = { type = \"object\", required = [\"id\", \"total\"] }\non_abstain = \"fail_closed\"\n\n# observe: every request is forwarded unchanged and a receipt is written off\n# the hot path. Nothing routes differently until mode = \"enforce\".\n",
      "enforce": "# firstpass.toml — written by `firstpass onboard`. Re-run onboard to regenerate.\n#   FIRSTPASS_MODE=enforce FIRSTPASS_CONFIG=./firstpass.toml firstpass up\n\n[[route]]                   # routes match top to bottom; first match wins\nmatch  = {}                 # everything\nmode   = \"enforce\"\nladder = [\"openai/gpt-4.1-mini\", \"openai/gpt-5.5\"]\ngates  = [\"json-valid\", \"extract-shape\"]\n\n[[gate]]\nid         = \"extract-shape\"\nschema     = { type = \"object\", required = [\"id\", \"total\"] }\non_abstain = \"fail_closed\"\n\n[escalation]\nmax_rungs_per_request = 2   # one rung up, never a runaway\n",
    },
    "code": {
      "observe": "# firstpass.toml — written by `firstpass onboard`. Re-run onboard to regenerate.\n#   FIRSTPASS_MODE=observe FIRSTPASS_CONFIG=./firstpass.toml firstpass up\n\n[[route]]                   # routes match top to bottom; first match wins\nmatch  = {}                 # everything\nmode   = \"observe\"\nladder = [\"openai/gpt-4.1-mini\", \"openai/gpt-5.5\"]\ngates  = [\"json-valid\", \"unit-tests\"]\n\n[[gate]]\n# REPLACE ME. A gate reads the candidate as JSON on stdin and prints\n#   {\"verdict\":\"pass|fail|abstain\", \"score\"?: 0.0-1.0, \"reason\"?: \"...\"}\n# on stdout. Wrap your real test command in that contract — a bare `cargo test`\n# or `npm test` will not work, it would abstain on every request.\n# `firstpass doctor` fails on this line until you point it at your wrapper.\nid  = \"unit-tests\"\ncmd = [\"your-test-runner\", \"--from-stdin\"]\n\n# observe: every request is forwarded unchanged and a receipt is written off\n# the hot path. Nothing routes differently until mode = \"enforce\".\n",
      "enforce": "# firstpass.toml — written by `firstpass onboard`. Re-run onboard to regenerate.\n#   FIRSTPASS_MODE=enforce FIRSTPASS_CONFIG=./firstpass.toml firstpass up\n\n[[route]]                   # routes match top to bottom; first match wins\nmatch  = {}                 # everything\nmode   = \"enforce\"\nladder = [\"openai/gpt-4.1-mini\", \"openai/gpt-5.5\"]\ngates  = [\"json-valid\", \"unit-tests\"]\n\n[[gate]]\n# REPLACE ME. A gate reads the candidate as JSON on stdin and prints\n#   {\"verdict\":\"pass|fail|abstain\", \"score\"?: 0.0-1.0, \"reason\"?: \"...\"}\n# on stdout. Wrap your real test command in that contract — a bare `cargo test`\n# or `npm test` will not work, it would abstain on every request.\n# `firstpass doctor` fails on this line until you point it at your wrapper.\nid  = \"unit-tests\"\ncmd = [\"your-test-runner\", \"--from-stdin\"]\n\n[escalation]\nmax_rungs_per_request = 2   # one rung up, never a runaway\n",
    },
    "prose": {
      "observe": "# firstpass.toml — written by `firstpass onboard`. Re-run onboard to regenerate.\n#   FIRSTPASS_MODE=observe FIRSTPASS_CONFIG=./firstpass.toml firstpass up\n\n[[route]]                   # routes match top to bottom; first match wins\nmatch  = {}                 # everything\nmode   = \"observe\"\nladder = [\"openai/gpt-4.1-mini\", \"openai/gpt-5.5\"]\ngates  = [\"non-empty\", \"judge\"]\n\n[[gate]]                    # judge sits OUTSIDE the ladder: maker != checker\nid    = \"judge\"\njudge = { model = \"anthropic/claude-haiku-4-5\", threshold = 0.7, rubric = \"The response fully and correctly resolves the request, with no errors.\" }\n\n# observe: every request is forwarded unchanged and a receipt is written off\n# the hot path. Nothing routes differently until mode = \"enforce\".\n",
      "enforce": "# firstpass.toml — written by `firstpass onboard`. Re-run onboard to regenerate.\n#   FIRSTPASS_MODE=enforce FIRSTPASS_CONFIG=./firstpass.toml firstpass up\n\n[[route]]                   # routes match top to bottom; first match wins\nmatch  = {}                 # everything\nmode   = \"enforce\"\nladder = [\"openai/gpt-4.1-mini\", \"openai/gpt-5.5\"]\ngates  = [\"non-empty\", \"judge\"]\n\n[[gate]]                    # judge sits OUTSIDE the ladder: maker != checker\nid    = \"judge\"\njudge = { model = \"anthropic/claude-haiku-4-5\", threshold = 0.7, rubric = \"The response fully and correctly resolves the request, with no errors.\" }\n\n[escalation]\nmax_rungs_per_request = 2   # one rung up, never a runaway\n",
    },
    "mixed": {
      "observe": "# firstpass.toml — written by `firstpass onboard`. Re-run onboard to regenerate.\n#   FIRSTPASS_MODE=observe FIRSTPASS_CONFIG=./firstpass.toml firstpass up\n\n[[route]]                   # routes match top to bottom; first match wins\nmatch  = {}                 # everything\nmode   = \"observe\"\nladder = [\"openai/gpt-4.1-mini\", \"openai/gpt-5.5\"]\ngates  = [\"non-empty\", \"uncertainty\"]\n\n[[gate]]                    # k samples; agreement becomes the score\nid          = \"uncertainty\"\nconsistency = { model = \"openai/gpt-4.1-mini\", k = 3, threshold = 0.6 }\n\n# observe: every request is forwarded unchanged and a receipt is written off\n# the hot path. Nothing routes differently until mode = \"enforce\".\n",
      "enforce": "# firstpass.toml — written by `firstpass onboard`. Re-run onboard to regenerate.\n#   FIRSTPASS_MODE=enforce FIRSTPASS_CONFIG=./firstpass.toml firstpass up\n\n[[route]]                   # routes match top to bottom; first match wins\nmatch  = {}                 # everything\nmode   = \"enforce\"\nladder = [\"openai/gpt-4.1-mini\", \"openai/gpt-5.5\"]\ngates  = [\"non-empty\", \"uncertainty\"]\n\n[[gate]]                    # k samples; agreement becomes the score\nid          = \"uncertainty\"\nconsistency = { model = \"openai/gpt-4.1-mini\", k = 3, threshold = 0.6 }\n\n[escalation]\nmax_rungs_per_request = 2   # one rung up, never a runaway\n",
    },
  },
  "google": {
    "json": {
      "observe": "# firstpass.toml — written by `firstpass onboard`. Re-run onboard to regenerate.\n#   FIRSTPASS_MODE=observe FIRSTPASS_CONFIG=./firstpass.toml firstpass up\n\n[[provider]]                # only anthropic + openai are built in\nid          = \"google\"\ndialect     = \"gemini\"\nbase_url    = \"https://generativelanguage.googleapis.com\"\napi_key_env = \"GEMINI_API_KEY\"\n\n[[route]]                   # routes match top to bottom; first match wins\nmatch  = {}                 # everything\nmode   = \"observe\"\nladder = [\"google/gemini-3.5-flash-lite\", \"google/gemini-3.6-flash\"]\ngates  = [\"json-valid\", \"extract-shape\"]\n\n[[gate]]\nid         = \"extract-shape\"\nschema     = { type = \"object\", required = [\"id\", \"total\"] }\non_abstain = \"fail_closed\"\n\n# observe: every request is forwarded unchanged and a receipt is written off\n# the hot path. Nothing routes differently until mode = \"enforce\".\n",
      "enforce": "# firstpass.toml — written by `firstpass onboard`. Re-run onboard to regenerate.\n#   FIRSTPASS_MODE=enforce FIRSTPASS_CONFIG=./firstpass.toml firstpass up\n\n[[provider]]                # only anthropic + openai are built in\nid          = \"google\"\ndialect     = \"gemini\"\nbase_url    = \"https://generativelanguage.googleapis.com\"\napi_key_env = \"GEMINI_API_KEY\"\n\n[[route]]                   # routes match top to bottom; first match wins\nmatch  = {}                 # everything\nmode   = \"enforce\"\nladder = [\"google/gemini-3.5-flash-lite\", \"google/gemini-3.6-flash\"]\ngates  = [\"json-valid\", \"extract-shape\"]\n\n[[gate]]\nid         = \"extract-shape\"\nschema     = { type = \"object\", required = [\"id\", \"total\"] }\non_abstain = \"fail_closed\"\n\n[escalation]\nmax_rungs_per_request = 2   # one rung up, never a runaway\n",
    },
    "code": {
      "observe": "# firstpass.toml — written by `firstpass onboard`. Re-run onboard to regenerate.\n#   FIRSTPASS_MODE=observe FIRSTPASS_CONFIG=./firstpass.toml firstpass up\n\n[[provider]]                # only anthropic + openai are built in\nid          = \"google\"\ndialect     = \"gemini\"\nbase_url    = \"https://generativelanguage.googleapis.com\"\napi_key_env = \"GEMINI_API_KEY\"\n\n[[route]]                   # routes match top to bottom; first match wins\nmatch  = {}                 # everything\nmode   = \"observe\"\nladder = [\"google/gemini-3.5-flash-lite\", \"google/gemini-3.6-flash\"]\ngates  = [\"json-valid\", \"unit-tests\"]\n\n[[gate]]\n# REPLACE ME. A gate reads the candidate as JSON on stdin and prints\n#   {\"verdict\":\"pass|fail|abstain\", \"score\"?: 0.0-1.0, \"reason\"?: \"...\"}\n# on stdout. Wrap your real test command in that contract — a bare `cargo test`\n# or `npm test` will not work, it would abstain on every request.\n# `firstpass doctor` fails on this line until you point it at your wrapper.\nid  = \"unit-tests\"\ncmd = [\"your-test-runner\", \"--from-stdin\"]\n\n# observe: every request is forwarded unchanged and a receipt is written off\n# the hot path. Nothing routes differently until mode = \"enforce\".\n",
      "enforce": "# firstpass.toml — written by `firstpass onboard`. Re-run onboard to regenerate.\n#   FIRSTPASS_MODE=enforce FIRSTPASS_CONFIG=./firstpass.toml firstpass up\n\n[[provider]]                # only anthropic + openai are built in\nid          = \"google\"\ndialect     = \"gemini\"\nbase_url    = \"https://generativelanguage.googleapis.com\"\napi_key_env = \"GEMINI_API_KEY\"\n\n[[route]]                   # routes match top to bottom; first match wins\nmatch  = {}                 # everything\nmode   = \"enforce\"\nladder = [\"google/gemini-3.5-flash-lite\", \"google/gemini-3.6-flash\"]\ngates  = [\"json-valid\", \"unit-tests\"]\n\n[[gate]]\n# REPLACE ME. A gate reads the candidate as JSON on stdin and prints\n#   {\"verdict\":\"pass|fail|abstain\", \"score\"?: 0.0-1.0, \"reason\"?: \"...\"}\n# on stdout. Wrap your real test command in that contract — a bare `cargo test`\n# or `npm test` will not work, it would abstain on every request.\n# `firstpass doctor` fails on this line until you point it at your wrapper.\nid  = \"unit-tests\"\ncmd = [\"your-test-runner\", \"--from-stdin\"]\n\n[escalation]\nmax_rungs_per_request = 2   # one rung up, never a runaway\n",
    },
    "prose": {
      "observe": "# firstpass.toml — written by `firstpass onboard`. Re-run onboard to regenerate.\n#   FIRSTPASS_MODE=observe FIRSTPASS_CONFIG=./firstpass.toml firstpass up\n\n[[provider]]                # only anthropic + openai are built in\nid          = \"google\"\ndialect     = \"gemini\"\nbase_url    = \"https://generativelanguage.googleapis.com\"\napi_key_env = \"GEMINI_API_KEY\"\n\n[[route]]                   # routes match top to bottom; first match wins\nmatch  = {}                 # everything\nmode   = \"observe\"\nladder = [\"google/gemini-3.5-flash-lite\", \"google/gemini-3.6-flash\"]\ngates  = [\"non-empty\", \"judge\"]\n\n[[gate]]                    # judge sits OUTSIDE the ladder: maker != checker\nid    = \"judge\"\njudge = { model = \"anthropic/claude-haiku-4-5\", threshold = 0.7, rubric = \"The response fully and correctly resolves the request, with no errors.\" }\n\n# observe: every request is forwarded unchanged and a receipt is written off\n# the hot path. Nothing routes differently until mode = \"enforce\".\n",
      "enforce": "# firstpass.toml — written by `firstpass onboard`. Re-run onboard to regenerate.\n#   FIRSTPASS_MODE=enforce FIRSTPASS_CONFIG=./firstpass.toml firstpass up\n\n[[provider]]                # only anthropic + openai are built in\nid          = \"google\"\ndialect     = \"gemini\"\nbase_url    = \"https://generativelanguage.googleapis.com\"\napi_key_env = \"GEMINI_API_KEY\"\n\n[[route]]                   # routes match top to bottom; first match wins\nmatch  = {}                 # everything\nmode   = \"enforce\"\nladder = [\"google/gemini-3.5-flash-lite\", \"google/gemini-3.6-flash\"]\ngates  = [\"non-empty\", \"judge\"]\n\n[[gate]]                    # judge sits OUTSIDE the ladder: maker != checker\nid    = \"judge\"\njudge = { model = \"anthropic/claude-haiku-4-5\", threshold = 0.7, rubric = \"The response fully and correctly resolves the request, with no errors.\" }\n\n[escalation]\nmax_rungs_per_request = 2   # one rung up, never a runaway\n",
    },
    "mixed": {
      "observe": "# firstpass.toml — written by `firstpass onboard`. Re-run onboard to regenerate.\n#   FIRSTPASS_MODE=observe FIRSTPASS_CONFIG=./firstpass.toml firstpass up\n\n[[provider]]                # only anthropic + openai are built in\nid          = \"google\"\ndialect     = \"gemini\"\nbase_url    = \"https://generativelanguage.googleapis.com\"\napi_key_env = \"GEMINI_API_KEY\"\n\n[[route]]                   # routes match top to bottom; first match wins\nmatch  = {}                 # everything\nmode   = \"observe\"\nladder = [\"google/gemini-3.5-flash-lite\", \"google/gemini-3.6-flash\"]\ngates  = [\"non-empty\", \"uncertainty\"]\n\n[[gate]]                    # k samples; agreement becomes the score\nid          = \"uncertainty\"\nconsistency = { model = \"google/gemini-3.5-flash-lite\", k = 3, threshold = 0.6 }\n\n# observe: every request is forwarded unchanged and a receipt is written off\n# the hot path. Nothing routes differently until mode = \"enforce\".\n",
      "enforce": "# firstpass.toml — written by `firstpass onboard`. Re-run onboard to regenerate.\n#   FIRSTPASS_MODE=enforce FIRSTPASS_CONFIG=./firstpass.toml firstpass up\n\n[[provider]]                # only anthropic + openai are built in\nid          = \"google\"\ndialect     = \"gemini\"\nbase_url    = \"https://generativelanguage.googleapis.com\"\napi_key_env = \"GEMINI_API_KEY\"\n\n[[route]]                   # routes match top to bottom; first match wins\nmatch  = {}                 # everything\nmode   = \"enforce\"\nladder = [\"google/gemini-3.5-flash-lite\", \"google/gemini-3.6-flash\"]\ngates  = [\"non-empty\", \"uncertainty\"]\n\n[[gate]]                    # k samples; agreement becomes the score\nid          = \"uncertainty\"\nconsistency = { model = \"google/gemini-3.5-flash-lite\", k = 3, threshold = 0.6 }\n\n[escalation]\nmax_rungs_per_request = 2   # one rung up, never a runaway\n",
    },
  },
  "local": {
    "json": {
      "observe": "# firstpass.toml — written by `firstpass onboard`. Re-run onboard to regenerate.\n#   FIRSTPASS_MODE=observe FIRSTPASS_CONFIG=./firstpass.toml firstpass up\n\n[[provider]]                # local rung; escalates to a frontier model\nid       = \"ollama\"\ndialect  = \"openai\"\nbase_url = \"http://localhost:11434\"   # keyless\n\n[[price]]                   # you host it, so the tokens really are free —\nmodel          = \"ollama/qwen2.5-coder:7b\"   # but the receipt has to SAY so,\ninput_per_mtok  = 0.0       # rather than infer $0.00 from a missing price\noutput_per_mtok = 0.0\n\n[[route]]                   # routes match top to bottom; first match wins\nmatch  = {}                 # everything\nmode   = \"observe\"\nladder = [\"ollama/qwen2.5-coder:7b\", \"anthropic/claude-sonnet-5\"]\ngates  = [\"json-valid\", \"extract-shape\"]\n\n[[gate]]\nid         = \"extract-shape\"\nschema     = { type = \"object\", required = [\"id\", \"total\"] }\non_abstain = \"fail_closed\"\n\n# observe: every request is forwarded unchanged and a receipt is written off\n# the hot path. Nothing routes differently until mode = \"enforce\".\n",
      "enforce": "# firstpass.toml — written by `firstpass onboard`. Re-run onboard to regenerate.\n#   FIRSTPASS_MODE=enforce FIRSTPASS_CONFIG=./firstpass.toml firstpass up\n\n[[provider]]                # local rung; escalates to a frontier model\nid       = \"ollama\"\ndialect  = \"openai\"\nbase_url = \"http://localhost:11434\"   # keyless\n\n[[price]]                   # you host it, so the tokens really are free —\nmodel          = \"ollama/qwen2.5-coder:7b\"   # but the receipt has to SAY so,\ninput_per_mtok  = 0.0       # rather than infer $0.00 from a missing price\noutput_per_mtok = 0.0\n\n[[route]]                   # routes match top to bottom; first match wins\nmatch  = {}                 # everything\nmode   = \"enforce\"\nladder = [\"ollama/qwen2.5-coder:7b\", \"anthropic/claude-sonnet-5\"]\ngates  = [\"json-valid\", \"extract-shape\"]\n\n[[gate]]\nid         = \"extract-shape\"\nschema     = { type = \"object\", required = [\"id\", \"total\"] }\non_abstain = \"fail_closed\"\n\n[escalation]\nmax_rungs_per_request = 2   # one rung up, never a runaway\n",
    },
    "code": {
      "observe": "# firstpass.toml — written by `firstpass onboard`. Re-run onboard to regenerate.\n#   FIRSTPASS_MODE=observe FIRSTPASS_CONFIG=./firstpass.toml firstpass up\n\n[[provider]]                # local rung; escalates to a frontier model\nid       = \"ollama\"\ndialect  = \"openai\"\nbase_url = \"http://localhost:11434\"   # keyless\n\n[[price]]                   # you host it, so the tokens really are free —\nmodel          = \"ollama/qwen2.5-coder:7b\"   # but the receipt has to SAY so,\ninput_per_mtok  = 0.0       # rather than infer $0.00 from a missing price\noutput_per_mtok = 0.0\n\n[[route]]                   # routes match top to bottom; first match wins\nmatch  = {}                 # everything\nmode   = \"observe\"\nladder = [\"ollama/qwen2.5-coder:7b\", \"anthropic/claude-sonnet-5\"]\ngates  = [\"json-valid\", \"unit-tests\"]\n\n[[gate]]\n# REPLACE ME. A gate reads the candidate as JSON on stdin and prints\n#   {\"verdict\":\"pass|fail|abstain\", \"score\"?: 0.0-1.0, \"reason\"?: \"...\"}\n# on stdout. Wrap your real test command in that contract — a bare `cargo test`\n# or `npm test` will not work, it would abstain on every request.\n# `firstpass doctor` fails on this line until you point it at your wrapper.\nid  = \"unit-tests\"\ncmd = [\"your-test-runner\", \"--from-stdin\"]\n\n# observe: every request is forwarded unchanged and a receipt is written off\n# the hot path. Nothing routes differently until mode = \"enforce\".\n",
      "enforce": "# firstpass.toml — written by `firstpass onboard`. Re-run onboard to regenerate.\n#   FIRSTPASS_MODE=enforce FIRSTPASS_CONFIG=./firstpass.toml firstpass up\n\n[[provider]]                # local rung; escalates to a frontier model\nid       = \"ollama\"\ndialect  = \"openai\"\nbase_url = \"http://localhost:11434\"   # keyless\n\n[[price]]                   # you host it, so the tokens really are free —\nmodel          = \"ollama/qwen2.5-coder:7b\"   # but the receipt has to SAY so,\ninput_per_mtok  = 0.0       # rather than infer $0.00 from a missing price\noutput_per_mtok = 0.0\n\n[[route]]                   # routes match top to bottom; first match wins\nmatch  = {}                 # everything\nmode   = \"enforce\"\nladder = [\"ollama/qwen2.5-coder:7b\", \"anthropic/claude-sonnet-5\"]\ngates  = [\"json-valid\", \"unit-tests\"]\n\n[[gate]]\n# REPLACE ME. A gate reads the candidate as JSON on stdin and prints\n#   {\"verdict\":\"pass|fail|abstain\", \"score\"?: 0.0-1.0, \"reason\"?: \"...\"}\n# on stdout. Wrap your real test command in that contract — a bare `cargo test`\n# or `npm test` will not work, it would abstain on every request.\n# `firstpass doctor` fails on this line until you point it at your wrapper.\nid  = \"unit-tests\"\ncmd = [\"your-test-runner\", \"--from-stdin\"]\n\n[escalation]\nmax_rungs_per_request = 2   # one rung up, never a runaway\n",
    },
    "prose": {
      "observe": "# firstpass.toml — written by `firstpass onboard`. Re-run onboard to regenerate.\n#   FIRSTPASS_MODE=observe FIRSTPASS_CONFIG=./firstpass.toml firstpass up\n\n[[provider]]                # local rung; escalates to a frontier model\nid       = \"ollama\"\ndialect  = \"openai\"\nbase_url = \"http://localhost:11434\"   # keyless\n\n[[price]]                   # you host it, so the tokens really are free —\nmodel          = \"ollama/qwen2.5-coder:7b\"   # but the receipt has to SAY so,\ninput_per_mtok  = 0.0       # rather than infer $0.00 from a missing price\noutput_per_mtok = 0.0\n\n[[route]]                   # routes match top to bottom; first match wins\nmatch  = {}                 # everything\nmode   = \"observe\"\nladder = [\"ollama/qwen2.5-coder:7b\", \"anthropic/claude-sonnet-5\"]\ngates  = [\"non-empty\", \"judge\"]\n\n[[gate]]                    # judge sits OUTSIDE the ladder: maker != checker\nid    = \"judge\"\njudge = { model = \"anthropic/claude-opus-4-8\", threshold = 0.7, rubric = \"The response fully and correctly resolves the request, with no errors.\" }\n\n# observe: every request is forwarded unchanged and a receipt is written off\n# the hot path. Nothing routes differently until mode = \"enforce\".\n",
      "enforce": "# firstpass.toml — written by `firstpass onboard`. Re-run onboard to regenerate.\n#   FIRSTPASS_MODE=enforce FIRSTPASS_CONFIG=./firstpass.toml firstpass up\n\n[[provider]]                # local rung; escalates to a frontier model\nid       = \"ollama\"\ndialect  = \"openai\"\nbase_url = \"http://localhost:11434\"   # keyless\n\n[[price]]                   # you host it, so the tokens really are free —\nmodel          = \"ollama/qwen2.5-coder:7b\"   # but the receipt has to SAY so,\ninput_per_mtok  = 0.0       # rather than infer $0.00 from a missing price\noutput_per_mtok = 0.0\n\n[[route]]                   # routes match top to bottom; first match wins\nmatch  = {}                 # everything\nmode   = \"enforce\"\nladder = [\"ollama/qwen2.5-coder:7b\", \"anthropic/claude-sonnet-5\"]\ngates  = [\"non-empty\", \"judge\"]\n\n[[gate]]                    # judge sits OUTSIDE the ladder: maker != checker\nid    = \"judge\"\njudge = { model = \"anthropic/claude-opus-4-8\", threshold = 0.7, rubric = \"The response fully and correctly resolves the request, with no errors.\" }\n\n[escalation]\nmax_rungs_per_request = 2   # one rung up, never a runaway\n",
    },
    "mixed": {
      "observe": "# firstpass.toml — written by `firstpass onboard`. Re-run onboard to regenerate.\n#   FIRSTPASS_MODE=observe FIRSTPASS_CONFIG=./firstpass.toml firstpass up\n\n[[provider]]                # local rung; escalates to a frontier model\nid       = \"ollama\"\ndialect  = \"openai\"\nbase_url = \"http://localhost:11434\"   # keyless\n\n[[price]]                   # you host it, so the tokens really are free —\nmodel          = \"ollama/qwen2.5-coder:7b\"   # but the receipt has to SAY so,\ninput_per_mtok  = 0.0       # rather than infer $0.00 from a missing price\noutput_per_mtok = 0.0\n\n[[route]]                   # routes match top to bottom; first match wins\nmatch  = {}                 # everything\nmode   = \"observe\"\nladder = [\"ollama/qwen2.5-coder:7b\", \"anthropic/claude-sonnet-5\"]\ngates  = [\"non-empty\", \"uncertainty\"]\n\n[[gate]]                    # k samples; agreement becomes the score\nid          = \"uncertainty\"\nconsistency = { model = \"ollama/qwen2.5-coder:7b\", k = 3, threshold = 0.6 }\n\n# observe: every request is forwarded unchanged and a receipt is written off\n# the hot path. Nothing routes differently until mode = \"enforce\".\n",
      "enforce": "# firstpass.toml — written by `firstpass onboard`. Re-run onboard to regenerate.\n#   FIRSTPASS_MODE=enforce FIRSTPASS_CONFIG=./firstpass.toml firstpass up\n\n[[provider]]                # local rung; escalates to a frontier model\nid       = \"ollama\"\ndialect  = \"openai\"\nbase_url = \"http://localhost:11434\"   # keyless\n\n[[price]]                   # you host it, so the tokens really are free —\nmodel          = \"ollama/qwen2.5-coder:7b\"   # but the receipt has to SAY so,\ninput_per_mtok  = 0.0       # rather than infer $0.00 from a missing price\noutput_per_mtok = 0.0\n\n[[route]]                   # routes match top to bottom; first match wins\nmatch  = {}                 # everything\nmode   = \"enforce\"\nladder = [\"ollama/qwen2.5-coder:7b\", \"anthropic/claude-sonnet-5\"]\ngates  = [\"non-empty\", \"uncertainty\"]\n\n[[gate]]                    # k samples; agreement becomes the score\nid          = \"uncertainty\"\nconsistency = { model = \"ollama/qwen2.5-coder:7b\", k = 3, threshold = 0.6 }\n\n[escalation]\nmax_rungs_per_request = 2   # one rung up, never a runaway\n",
    },
  },
};
